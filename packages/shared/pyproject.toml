[project]
name = "shared"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Tibor Enyedi", email = "tibor.enyedi1996@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pika>=1.3.2",
]

[build-system]
requires = ["uv_build>=0.11.1,<0.12.0"]
build-backend = "uv_build"
