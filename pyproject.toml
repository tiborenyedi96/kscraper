[project]
name = "kscraper"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
    "services/scraper",
    "services/queue-consumer",
    "services/etl",
    "services/api",
    "packages/shared",
]
